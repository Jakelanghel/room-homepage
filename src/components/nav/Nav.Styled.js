import styled from "styled-components";

export const StyledNav = styled.nav`
  width: 100%;
  display: flex;
  padding: 3rem 2rem;

  position: fixed;
  z-index: 1;

  .container-logo {
    margin: 0 auto;
  }

  .container-links {
    display: none;
  }
`;
